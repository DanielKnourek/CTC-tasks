FROM alpine:3.15.4

COPY ./client.app ./client.app
COPY ./static ./static

ARG GRPC_PORT_ARG=50051
ARG GRPC_ADDRESS_ARG=localhost
ARG CLIENT_PORT_ARG=8080

ENV GRPC_PORT=${GRPC_PORT_ARG}
ENV GRPC_ADDRESS=${GRPC_ADDRESS_ARG}
ENV CLIENT_PORT_ARG=${CLIENT_PORT_ARG}

ENTRYPOINT ["/client.app"]


