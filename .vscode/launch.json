{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Server - current file",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "console": "integratedTerminal",
            "program": "${workspaceFolder}/${relativeFile}"
        }
    ]
}